export class Repository {
    constructor(public name: string, public full_name: string, public description: string, public html_url: string, public language: string, public watching: number, public updated_at:Date, public created_at:Date){}
}
