import { DateCheckPipe } from './date-check.pipe';

describe('DateCheckPipe', () => {
  it('create an instance', () => {
    const pipe = new DateCheckPipe();
    expect(pipe).toBeTruthy();
  });
});
